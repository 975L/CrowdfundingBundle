{# Where the counterparts are going - empty on a contribution that ships nothing, which renders no gap #}
{% if basket is defined and basket %}<twig:c975LPayment:Basket:Delivery basket="{{ basket }}"/>{% endif %}
