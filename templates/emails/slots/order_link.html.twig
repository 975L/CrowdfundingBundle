{# The order number, linking to the page that tracks the contribution #}
{% if basket is defined and basket %}<twig:c975LPayment:Basket:Number number="{{ basket.number }}" securityToken="{{ basket.securityToken }}"/>{% endif %}
