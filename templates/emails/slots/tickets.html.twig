{# Every ticket the contribution earned, in one email rather than one email per ticket #}
{% if tickets is defined and tickets is not empty %}<twig:c975LCrowdfunding:Lottery:Tickets tickets="{{ tickets }}"/>{% endif %}
