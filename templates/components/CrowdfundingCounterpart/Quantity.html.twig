<hr>
<div class="limited-quantity">
    {% if null != limited %}
        <p class="primary bold">{{ 'label.limited_quantity'|trans({}, 'crowdfunding') }} : {{ limited }}</p>
        <p>{{ 'label.ordered_quantity'|trans({}, 'crowdfunding') }} : <strong>{{ ordered ?? 0 }}</strong></p>
        <p>{{ 'label.remaining_quantity'|trans({}, 'crowdfunding') }} : <strong>{{ limited - ordered }}</strong></p>
    {% else %}
        <p>{{ 'label.ordered_quantity'|trans({}, 'crowdfunding') }} : <strong>{{ ordered ?? 0 }}</strong></p>
    {% endif %}
    <p>{{ 'label.expected_delivery'|trans({}, 'crowdfunding') }} : <strong>{{ expectedDelivery }}</strong></p>
</div>
<hr>