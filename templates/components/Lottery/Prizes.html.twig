<section class="cards">
    {% for prize in prizes %}
        {% set winner = false %}
        {% if prize.winningTicket %}
            {% set winner = true %}
        {% endif %}
        <twig:c975LCrowdfunding:Lottery:Prize prize="{{ prize }}" winner="{{ winner }}"/>
    {% endfor %}
</section>