<article class="crowdfunding {{ class|default("") }}" id="crowdfunding-{{ crowdfunding.id }}">
    {% set imageSrc = crowdfunding.medias|first != false ? crowdfunding.medias|first : "bundles/c975lcrowdfunding/images/no-product-image.webp" %}
    <twig:c975LUi:Image:Link src="{{ asset(imageSrc) }}" url="{{ path('crowdfunding_display', {'slug': crowdfunding.slug}) }}" class="img-shadow img-crowdfunding" width="150"/>
    <h3>{{ crowdfunding.title }}</h3>
</article>